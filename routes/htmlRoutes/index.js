const router = require('express').Router();

// use sendfile command for each route to send the index.html and the notes.html to the server

// /notes route
// * (star) route

//at bottom of file
module.exports = router;